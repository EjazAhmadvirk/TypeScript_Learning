// Explicit type annotation
let message: string = "Hello, TypeScript!";

// Type inference (TS automatically infers string type)
let greeting = "Hi there!";

// Using single quotes, double quotes, or template literals
let singleQuote: string = 'Hello';
let doubleQuote: string = "World";
let templateLiteral: string = `Hi, ${singleQuote} ${doubleQuote}!`;
